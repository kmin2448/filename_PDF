PRD: PDF Auto-Renamer
1. 프로젝트 개요
이름: PDF Auto-Renamer

한 줄 설명: 업로드한 PDF 파일 내부의 텍스트를 추출해 지정된 규칙에 맞게 파일명을 자동 변경하고 목표 폴더로 분류하는 데스크톱 GUI 프로그램

유형: 로컬 데스크톱 애플리케이션 (GUI)

난이도: 초~중급

2. 사용자 시나리오
누가: 예산 집행 및 증빙 내역을 관리하는 실무 담당자

언제: 다수의 집행 증빙용 청구서(PDF)를 다운로드하여 정해진 규칙에 따라 파일명을 일괄 변경하고 비목별 폴더에 정리해야 할 때

왜: 매번 파일을 열어 특정 페이지의 고정된 정보를 확인하고 파일명을 타이핑하는 단순 반복 루틴을 자동화하여, 작업 시간을 획기적으로 단축하고 업무 효율을 높이기 위해

3. 핵심 기능 목록
[필수 기능]

PDF 파일 다중 업로드: 드래그 앤 드롭 및 파일 탐색기를 통한 파일 추가

지정 페이지 맞춤형 데이터 추출 (파싱):

[1페이지] 이체일자: 텍스트 추출 후 YYMMDD 형식으로 변환 (예: 2026-03-06 -> 260306)

[1페이지] 적요: 해당 란에 기재된 텍스트 내용 전체 추출

[1페이지] 비목 및 금회청구액: 하단 예산 표를 분석하여 '금회청구액'란에 금액이 존재하는 행을 찾고, 해당 행의 '비목'과 '금회청구액' 데이터 추출

[3페이지] 거래처명(성명): 지급명세서 표 구조를 인식하여 '거래처명(성명)' 헤더 아래 행에 있는 텍스트 추출

자동 파일명 생성: (YYMMDD) 적요_거래처명(성명)_(금회청구액).pdf 형식으로 문자열 조합

스마트 폴더 분류 및 저장:

저장 경로: 데스크톱(바탕화면)/청구서정리/[추출된 비목]/

해당 '비목' 폴더가 없으면 새로 생성, 있으면 해당 폴더 내부에 파일 복사 (원본 파일 유지)

작업 결과 안내: 파일 처리 완료 시 "OOO 폴더에 저장되었습니다." 팝업 안내창 출력

[선택 기능]

수동 파일명 수정 기능: 정보 추출 실패 시, 사용자가 목록(UI)에서 직접 파일명을 타이핑하여 수정하고 저장할 수 있는 기능

폴더 열기 지원: 처리가 완료된 항목에 대해 결과물이 저장된 폴더를 즉시 열어볼 수 있는 기능

4. 기술 스택
프로그래밍 언어: Python 3.x

GUI 프레임워크: PyQt6 또는 PySide6

PDF 파싱 라이브러리: pdfplumber (표 구조 및 특정 페이지 텍스트 정밀 추출)

실행 파일 패키징: PyInstaller

5. 화면 구성
상단 영역 (입력부): 드래그 앤 드롭 영역 및 [파일 추가] 버튼

중단 영역 (목록부 - Table): * 컬럼: 원본 파일명 | 변경될 파일명 (더블클릭 시 텍스트 수정 가능) | 상태 (대기/완료/에러) | 폴더 열기 (버튼)

하단 영역 (제어부):

[작업 시작] 버튼, [목록 초기화] 버튼

작업 진행 상태를 나타내는 프로그레스 바 (Progress Bar)

'원본 파일 유지' 고정 안내 텍스트

6. 상세 기능 명세
파일 로드: 사용자가 업로드한 PDF가 목록에 '대기' 상태로 등록됨

데이터 추출: 1페이지(이체일자, 적요, 비목, 금회청구액)와 3페이지(거래처명)에서 지정된 규칙에 따라 텍스트 및 표 데이터를 파싱

파일명 조합: 파싱된 데이터를 조합하여 목표 파일명 문자열 생성

폴더 확인 및 생성: 데스크톱/청구서정리/[비목] 경로를 확인하여 필요시 디렉토리 생성

파일 복사 및 로깅: 원본 파일은 그대로 둔 채, 대상 폴더에 새 파일명으로 복사. 완료 시 팝업창을 띄우고, UI 목록의 상태를 '완료'로 변경 후 [폴더 열기] 버튼 활성화. 파싱 실패 시 '에러' 상태로 표시하여 수동 수정 유도

7. 디자인 가이드
전체 테마: 다크 모드 전용 (사용자 지정 다크 그레이 배경색 사용)

색상 팔레트: 배경은 다크 그레이, 텍스트/포인트는 화이트 및 라이트 그레이. 블루 톤 및 원색 계열 사용 금지

타이포그래피: 가독성 높은 고딕 계열 (Pretendard 등) 사용, 이모지 사용 전면 금지

UI 컴포넌트 효과: 투명도(Opacity)가 조절된 창을 사용하여 공간감 부여, 그림자 효과(Drop Shadow)는 필수적인 입체감 표현에만 최소한으로 사용

8. 제약사항
OS 환경: Windows 10 이상 환경에서 구동

보안: 외부 서버나 인터넷 연결 없이 100% 로컬 환경에서만 동작 (데이터 유출 원천 차단)

파일 양식: 규격화된 특정 청구서 양식(1페이지 지출요구 및 결의서, 3페이지 지급명세서)을 벗어나는 PDF의 경우 정상적인 자동 파싱을 보장하지 않음

용량 제한: 한 번에 업로드할 수 있는 PDF 파일의 수는 최대 50개로 제한